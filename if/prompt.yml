--- # prompt for user package
 - hosts: localhost
   sudo: yes
   gather_facts: no
   vars:
     pkgname: httpd
   tasks:
     - name: install indicated {{ pkgname }}
       yum: name={{ pkgname }} state=latest
       register: results
     - name: debug out put
       debug: var=results
